[package]
name = "hold"
version = "0.1.0"
edition = "2024"

[dependencies]
rfd = { version = "0.17", default-features = false, features = ["xdg-portal"]}
csv = "1.4"
serde = { version = "1.0", features = ["derive"]}
ordered-float = "5.0"
